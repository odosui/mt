{
  "version": "1.1.6",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "NODE_ENV=production npm run start --prefix server",
    "e2e": "playwright test",
    "tmp-start": "NODE_ENV=production MT_PORT=3344 MT_HOME=../mt-tmp NODE_ENV=production npm run start --prefix server",
    "docs-start": "NODE_ENV=production MT_PORT=3345 MT_HOME=../docs NODE_ENV=production npm run start --prefix server",
    "docs-build": "MT_HOME=./docs npx ts-node --project server/tsconfig.json server/src/index.ts --build-static-site ../mt-docs-static",
    "docs-serve": "npx serve docs-static -l 3333"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1",
    "oxlint": "^1.32.0",
    "prettier": "^3.4.2",
    "typescript": "^6.0.2",
    "vitest": "^4.0.16"
  }
}
